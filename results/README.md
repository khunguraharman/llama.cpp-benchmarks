# Benchmark results

Place raw llama.cpp benchmark JSON files in the folder for the machine that produced them.

- `nvidia-rtx-3080/`: Windows desktop results from the NVIDIA GeForce RTX 3080.
- `m4-mac-mini/`: Apple M4 Mac Mini results.

The plotting script discovers JSON files under this directory by default.
